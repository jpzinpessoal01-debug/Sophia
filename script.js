const intro=document.getElementById('intro');document.getElementById('enter').addEventListener('click',()=>{intro.classList.add('hidden');document.body.style.overflow='auto';setTimeout(()=>intro.remove(),1000)});document.body.style.overflow='hidden';const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));function timer(){const start=new Date('2026-06-11T00:00:00-03:00').getTime(),d=Math.max(0,Date.now()-start),days=Math.floor(d/86400000),hours=Math.floor(d%86400000/3600000),mins=Math.floor(d%3600000/60000),secs=Math.floor(d%60000/1000);daysEl.textContent=days;hoursEl.textContent=String(hours).padStart(2,'0');minsEl.textContent=String(mins).padStart(2,'0');secsEl.textContent=String(secs).padStart(2,'0')}const daysEl=document.getElementById('days'),hoursEl=document.getElementById('hours'),minsEl=document.getElementById('mins'),secsEl=document.getElementById('secs');timer();setInterval(timer,1000);document.querySelectorAll('.letter').forEach(el=>el.addEventListener('click',()=>el.classList.toggle('open')));document.addEventListener('mousemove',e=>document.querySelectorAll('.tilt').forEach(el=>{const r=el.getBoundingClientRect();if(r.top>innerHeight||r.bottom<0)return;const x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${-y*7}deg) rotateY(${x*9}deg) translateY(-4px)`}));const c=document.getElementById('stars'),ctx=c.getContext('2d');let pts=[];function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);pts=Array.from({length:Math.min(130,Math.floor(innerWidth/7))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.6+.3,s:Math.random()*.22+.04,a:Math.random()*.5+.15}))}resize();addEventListener('resize',resize);(function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);pts.forEach(p=>{p.y-=p.s;if(p.y<-3)p.y=innerHeight+3;ctx.beginPath();ctx.fillStyle=`rgba(255,190,215,${p.a})`;ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()});requestAnimationFrame(draw)})();